#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  keyboard:
    layout: us
  identity:
    hostname: k3s-master
    username: geriatricgoose
    password: "$1$MB49ihyN$1X5XbNmANggN2DRQSwbXu1"
  ssh:
    install-server: true
    allow-pw: true
  network:
    version: 2
    ethernets:
      eth_all:
        match:
          name: "e*"
        dhcp4: true
  storage:
    layout:
      name: direct
  late-commands:
    - curtin in-target --target=/target -- sed -i 's/^GRUB_CMDLINE_LINUX_DEFAULT=.*/GRUB_CMDLINE_LINUX_DEFAULT="quiet pcie_aspm=off pcie_port_pm=off"/' /etc/default/grub
    - curtin in-target --target=/target -- update-grub
